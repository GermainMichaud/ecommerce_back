import { createColor } from './color.service';
import { createProduct, getProduct, getProducts } from './product.service';
import { createSeller } from './seller.service';
import { createSize } from './size.service';
import { createVariant } from './variant.service';

export {
  createColor,
  createProduct,
  createSeller,
  createSize,
  createVariant,
  getProduct,
  getProducts,
};
